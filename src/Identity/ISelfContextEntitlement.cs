namespace Sufficit.Identity
{
    /// <summary>
    /// Marks entitlements whose empty context is resolved to the authenticated user's own ID.
    /// </summary>
    public interface ISelfContextEntitlement
    {
    }
}
