namespace Sufficit.Identity
{
    /// <summary>
    /// Marks directives whose empty context is resolved to the authenticated user's own ID.
    /// </summary>
    public interface ISelfContextDirective
    {
    }
}
